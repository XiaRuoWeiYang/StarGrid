# 第二版验证记录

日期：2026-09-18。运行环境：Windows、Node.js、Codex 内置浏览器。本轮替代第一版单关、X 和撤销的验收记录。

## 纯逻辑测试
运行 `node Tests/verify.cjs`，通过。
- 普通关答案数依次为 720、90、3、2、1；独立枚举所有列排列并与规则胜利结果交叉核对。
- 验证所有普通关合法解及其前缀：提示保留可扩展的玩家路径，不修改输入，不把另一合法解误判为错。
- 冲突边界、跨行不误判；从全星错误棋盘反复提示最终可完成。
- 北京时间午夜切换、31 天每日样本：简单多解、困难唯一解、区域连通且每区至少三格、固定线索与提示收敛。
- 相同日期重建一致；31 天得到 30 种困难布局，5 次兜底，未承诺永不重复。
- 损坏/旧版本存档、设置类型、非法棋盘恢复校验。

## 浏览器检查
已通过开始页面→普通关、直接放星/再次取消、同一入门关正对角线和反对角线两种摆法均通关、结算下一关进入第二关、重玩确认、欣赏棋盘后仍可继续。

已通过设置开关、刷新后设置保留、每日简单/困难入口与当天日期显示。后续本轮检查结果继续记录于此。

## 限制
尚未在实体 iOS/Android 手机上验证；难度未经过玩家测试。音效可用性受设备和浏览器音频策略影响。当前运行检查采用本机 HTTP，file:// 依赖相对脚本和样式且未使用 fetch。存档只在当前浏览器同源内保存，无跨设备同步。
